[Unit]
Description=pmacctd nfprobe
After=network.target

[Service]
Type=forking
EnvironmentFile=-/etc/default/pmacctd_nfprobe
ExecStart=/usr/sbin/pmacctd -f ${PMACCTD_NFPROBE_CONF} $DAEMON_OPTS

[Install]
WantedBy=multi-user.target
