[Unit]
Description=netflow accounting daemon - backend
After=network.target

[Service]
Type=forking
EnvironmentFile=-/etc/default/nfacctd_backend
ExecStart=/usr/sbin/nfacctd -f ${NFACCTD_BACKEND_CONF} $DAEMON_OPTS

[Install]
WantedBy=multi-user.target
